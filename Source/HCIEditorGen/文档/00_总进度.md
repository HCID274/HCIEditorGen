# HCIAbilityKit 总进度（权威）

> 项目根目录：`D:\1_Projects\04_GameDev\UE_Projects\HCIEditorGen`  
> 引擎版本：`UE 5.4.4`  
> 更新时间：2026-02-24  
> 当前阶段：`Stage G`  
> 当前切片：`Stage G-SliceG10（DocFreeze 已冻结，待实现）`

## 1. 当前结论（先看这个）

- 主线未偏：仍是 `HCIAbilityKit` 资产审计主链路 + Agent 安全编排。
- 已完成到：`Stage G-SliceG9`（含用户 UE 手测 Pass）。
- 当前阻塞点：`G10` 已完成文档冻结，当前待实现与 UE 手测门禁。
- 执行纪律不变：每个切片完成后必须停下，等待用户 UE 手测 `Pass`。

## 2. 范围冻结（一期）

- 仅做 `HCIAbilityKit`，不扩展无关系统。
- 架构冻结：`HCIAbilityKitRuntime + HCIAbilityKitEditor`，依赖单向 `Editor -> Runtime`。
- 工作流冻结：`计划 -> 执行 -> 审阅`。
- 安全冻结：`Confirm`、`Blast Radius(50)`、`All-or-Nothing`、`SC Fail-Fast`、`Mock RBAC`、`LOD Nanite 拦截`。
- 交互冻结：Actor 走 `Camera Focus`，纯资产走 `SyncBrowserToObjects`。

## 3. 阶段进度快照

| 阶段 | 状态 | 备注 |
|---|---|---|
| Step1 结构迁移 | 已关闭 | 通过 |
| Step2 服务抽离 | 已关闭 | 通过 |
| Step3 检索实验线 | 历史基线 | 保留，不作为当前主线门禁 |
| Stage A | 已完成 | 文档与契约冻结 |
| Stage B | 已完成 | 种子资产 + 异步扫描 + 预览体链路 |
| Stage C | 已完成 | RuleRegistry 首批规则 + 统一报告 + JSON 导出 |
| Stage D | 已完成 | 深度检查、GC 节流、吞吐与内存监控 |
| Stage E | 已完成 | ToolRegistry + 安全门禁链路 |
| Stage F | 已收官 | F1~F15 全通过 |
| Stage G | 进行中 | G1~G9 通过，G10 文档已冻结待实现 |

## 4. 已完成里程碑（简版）

- B0~B8：`RepresentingMesh` 链路、扫描异步化、跳过留痕、预览体与自动同步。
- C1~C4：规则框架、首批规则（`TriangleExpectedMismatch/TextureNPOT/HighPolyAutoLODRule`）、统一结果结构、JSON 导出。
- D1~D3：深度模式、GC 节流、`Perf/PerfSummary` 指标。
- E1~E8：7 工具白名单、Dry-Run Diff、确认门禁、限流、事务、SC、RBAC、LOD 安全。
- F1~F15：`Plan -> Validate -> Execute(simulate_dry_run) -> Review -> Apply/Confirm/Ticket -> SimArchive/Handoff` 完整闭环。
- G1~G9：Stage G dry-run 桥接链路推进到 `StageGExecuteArchiveBundle` 并完成手测。

## 5. 当前切片定义（G10）

- 切片编号：`Stage G-SliceG10`
- 当前状态：`DocFreeze 已冻结（待实现，禁止越片）`
- 本片冻结结论：
  1. `G10` 继续 `simulate_dry_run`，定位为“执行就绪报告”收口，不做真实写入。
  2. 冻结 `StageGExecutionReadinessReport` 输入/输出字段与错误码（`E4218/E4219`）。
  3. 冻结 UE 手测 5 案例（成功 + 未确认 + digest 篡改 + archive 未就绪 + mode 非法）。
- 下一动作：进入 `G10-Impl`（实现命令 + 编译 + UE 手测）。
- 后续推进规则（固定）：`G10 Pass -> 文档定义 H1 -> 再实现 H1`，禁止跳过文档直接开新切片。

## 5.1 加速推进快照（冻结）

- 当前：`G10-Impl`（文档已冻结，进入实现与门禁）。
- 下一步（`G10 Pass` 后）：
  1. `H1`：接入 LLM API 到 Planner（保持 `simulate_dry_run`）。
  2. `H2`：LLM 超时/空响应/非法结构的降级回退。
  3. `I1`：最小 UI（Plan 预览 + DryRunDiff + Locate）。
- 节奏策略：每片按 2 天窗口推进（冻结 0.5d + 实现 1d + UE 门禁 0.5d），未 Pass 不跨片。

## 6. 风险与收束

- 风险：计划文档之间口径不一致导致反复返工。
  - 收束：以 `00/01/05` 三文档为唯一计划闭环；任何里程碑变更先改文档。
- 风险：在 `G10` 已冻结前后口径混用（待定义/已冻结并存）导致执行混乱。
  - 收束：当前统一口径为“G10 DocFreeze 已冻结，待实现与 UE 门禁”。
- 风险：主线信息膨胀导致协作成本上升。
  - 收束：详细证据留在 `测试记录/`，`00` 只保留权威快照与下一步。

## 7. 权威文档入口

- 技术方案：`Source/HCIEditorGen/文档/01_技术方案_插件双模块.md`
- 执行主计划：`Source/HCIEditorGen/文档/05_开发执行总方案_资产审计.md`
- 测试门禁：`Source/HCIEditorGen/文档/02_测试门禁与节奏.md`
- 契约规范：`Source/HCIEditorGen/文档/03_Schema_v1与错误契约.md`
- 测试记录目录：`Source/HCIEditorGen/文档/测试记录/`
