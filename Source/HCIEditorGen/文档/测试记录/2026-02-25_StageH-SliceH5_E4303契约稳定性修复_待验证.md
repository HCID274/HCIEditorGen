# StageH-SliceH5_E4303契约稳定性修复（已通过）

- 日期：2026-02-25
- 负责人：Codex + 用户 UE 手测
- 目标：降低 `E4303(llm_contract_invalid)` 触发，确保“整理临时目录”链路优先走真实 LLM 契约输出。

## 1. 测试步骤

1. 执行命令：`HCI.AgentPlanWithRealLLMDemo "整理临时目录资产并归档"`
2. 执行命令：`HCI.AgentPlanPreviewUI "整理临时目录资产并归档"`
3. 如出现 fallback，检查日志：`[HCIAbilityKit][AgentPlanLLM][E4303] ... detail=...`

## 2. 预期结果

1. `AgentPlanWithRealLLMDemo` 命中：`provider=llm`、`provider_mode=real_http`、`fallback_used=false`。
2. `AgentPlanPreviewUI` 命中：`SearchPath -> ScanAssets` discovery 链路，窗口正常打开。
3. 若失败，日志包含具体字段级错误（`code/field/reason`），不再只有笼统 `E4303`。

## 3. 实际结果

1. `HCI.AgentPlanWithRealLLMDemo "整理临时目录资产并归档"`：
   - 命中 `provider=llm`、`provider_mode=real_http`、`fallback_used=false`、`error_code=-`。
   - 路由结果为 `step_1_search -> step_2_scan`，无 `llm_contract_invalid` 回退。
2. `HCI.AgentPlanPreviewUI "整理临时目录资产并归档"`：
   - 命中 `provider=llm`、`provider_mode=real_http`、`fallback_used=false`。
   - 预览窗口正常打开，步骤为 `SearchPath` 后接 `ScanAssets`，参数使用管道变量。

## 4. 证据

- `AgentPlanWithRealLLMDemo` 摘要日志：
  - `provider=llm provider_mode=real_http fallback_used=false ... route_reason=naming_traceability_temp_assets_scan_first_step_order_normalized steps=2`
- `AgentPlanPreviewUI` 摘要日志：
  - `provider=llm provider_mode=real_http fallback_used=false ... route_reason=naming_traceability_temp_assets_scan_first_step_order_normalized steps=2`
- 步骤日志（两条链路一致）：
  - `row=0 step_id=step_1_search tool_name=SearchPath args={"keyword":"Temp"}`
  - `row=1 step_id=step_2_scan tool_name=ScanAssets args={"directory":"{{step_1_search.matched_directories[0]}}"}`

## 5. 结论

- `Pass`（2026-02-25）。
- H5 目标达成：`E4303` 在“整理临时目录资产并归档”主链路不再触发降级，本片关闭。
