# HCIAbilityKit 开发执行总方案（资产审计主线）

> 状态：冻结（V6）  
> 更新时间：2026-02-24  
> 适用范围：`HCIAbilityKit`

## 1. 执行原则

1. 文档冻结先于代码实现。
2. 每片完成后必须等待用户 UE 手测 `Pass`。
3. 未通过门禁不得跨切片推进。
4. 保持 `simulate_dry_run` 直到明确开放真实执行。

## 2. 阶段状态

- `Stage A~D`：已完成。
- `Stage E`：已完成。
- `Stage F`：已完成（F1~F15）。
- `Stage G`：已完成（G1~G10，用户 UE 手测 Pass）。
- `Stage H`：进行中（H1 已通过 UE 手测，等待下一切片指令）。

## 3. 当前切片：Stage H-SliceH1（已通过 UE 手测）

### 3.1 本片只做什么

- 接入 LLM Planner Provider，增强 `NL -> Plan JSON` 生成。
- 保留现有关键词规划器作为回退路径。
- 统一输出 Planner 结果元数据（provider/fallback）。

### 3.2 本片不做什么

- 不修改 ToolRegistry 白名单。
- 不修改 ExecutionGate 语义。
- 不引入真实写执行，不做审批 UI。

### 3.3 实现边界（代码级）

- Runtime：新增 Planner Provider 抽象与 LLM Provider 接口。
- Runtime：保留 Keyword Planner，并作为回退实现。
- Editor：新增 H1 演示命令，用于验证成功/回退/失败路径。
- 全链路仍需经过：`PlanValidator -> Executor(simulate_dry_run)`。

### 3.4 H1 验收日志口径（唯一判据）

- 成功：
  - `planner_provider=llm`
  - `plan_validation=ok`
- 回退：
  - `planner_provider=keyword_fallback`
  - `fallback_reason in {llm_timeout,llm_invalid_json,llm_contract_invalid,llm_empty_response}`
- 安全：
  - 非白名单/风险不合法仍返回 `E4002/E4003`
  - 不发生真实写入

### 3.5 H1 预定 UE 命令（实现后执行）

- `HCIAbilityKit.AgentPlanWithLLMDemo "整理临时目录资产并归档"`
- `HCIAbilityKit.AgentPlanWithLLMDemo "扫描关卡里缺碰撞和默认材质"`
- `HCIAbilityKit.AgentPlanWithLLMDemo "把高面数网格做LOD检查"`
- `HCIAbilityKit.AgentPlanWithLLMFallbackDemo timeout`
- `HCIAbilityKit.AgentPlanWithLLMFallbackDemo invalid_json`
- `HCIAbilityKit.AgentPlanWithLLMFallbackDemo empty`

### 3.6 H1 手测结果（2026-02-24）

- `正常资产归档`：`provider=llm`，`intent=normalize_temp_assets_by_metadata`，`PASS`。
- `正常关卡扫描`：`provider=llm`，`intent=scan_level_mesh_risks`，`PASS`。
- `正常 LOD 检查`：`provider=llm`，`intent=batch_fix_asset_compliance`，`PASS`。
- `超时回退`：`provider=keyword_fallback`，`fallback_reason=llm_timeout`，`E4301`，`PASS`。
- `损坏 JSON 回退`：`provider=keyword_fallback`，`fallback_reason=llm_invalid_json`，`E4302`，`PASS`。
- `空回复回退`：`provider=keyword_fallback`，`fallback_reason=llm_empty_response`，`E4304`，`PASS`。
- 结论：`Stage H-SliceH1 Pass`，本切片门禁关闭；按流程停在文档状态，等待下一切片指令。

## 4. 下一切片预案（仅占位，不提前实现）

### H2（紧随 H1）

- 目标：LLM 稳定性策略完善（超时、重试、熔断、指标）。
- 门禁：成功/回退/失败三类路径可观测可复现。

### I1（紧随 H2）

- 目标：最小只读 UI（Plan 预览 + DryRunDiff + Locate）。
- 门禁：用户可完成“看计划 -> 看差异 -> 定位”闭环。

## 5. 变更纪律

- 范围变更先改 `00 + 05`。
- 契约变更先改 `03`。
- 结构变更才改 `04`。
- 每次门禁结果必须记录到 `文档/测试记录/`。
