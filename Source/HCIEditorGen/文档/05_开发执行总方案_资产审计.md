# HCIAbilityKit 开发执行总方案（资产审计主线）

> 状态：冻结（V6）  
> 更新时间：2026-02-24  
> 适用范围：`HCIAbilityKit`

## 1. 执行原则

1. 文档冻结先于代码实现。
2. 每片完成后必须等待用户 UE 手测 `Pass`。
3. 未通过门禁不得跨切片推进。
4. 保持 `simulate_dry_run` 直到明确开放真实执行。

## 2. 阶段状态

- `Stage A~D`：已完成。
- `Stage E`：已完成。
- `Stage F`：已完成（F1~F15）。
- `Stage G`：已完成（G1~G10，用户 UE 手测 Pass）。
- `Stage H`：进行中（H1/H2 已通过；H3 启动）。

## 3. 当前切片：Stage H-SliceH3（文档冻结）

### 3.1 本片只做什么

- 真实接入 LLM Provider：`C++ + FHttpModule`，请求 OpenAI Compatible Chat Completions。
- 认证改为本地配置文件读取：`Saved/HCIAbilityKit/Config/llm_provider.local.json`。
- 保留现有关键词规划器 fallback 与 H2 的 `retry/circuit/metrics` 机制。
- 全链路继续 `PlanValidator -> Executor(simulate_dry_run)`。

### 3.2 本片不做什么

- 不修改 ToolRegistry 白名单。
- 不修改 ExecutionGate 语义。
- 不引入真实写执行，不做审批 UI。
- 不引入 Python 主调用链，不走外部脚本桥接。

### 3.3 实现边界（代码级）

- Runtime：新增真实 HTTP Provider（DashScope OpenAI compatible endpoint）。
- Runtime：新增 LLM 响应解析（`choices[0].message.content -> Plan JSON`），并落到现有 `FHCIAbilityKitAgentPlan`。
- Runtime：新增配置缺失与 HTTP 失败收敛口径（错误码 + fallback）。
- Editor：新增真实 LLM 演示命令（用于 UE 手测）。

### 3.4 H3 验收日志口径（唯一判据）

- 成功：
  - `planner_provider=llm`
  - `provider_mode=real_http`
  - `plan_validation=ok`
- 回退（配置）：
  - `planner_provider=keyword_fallback`
  - `fallback_reason=llm_config_missing`
- 回退（运行时）：
  - `planner_provider=keyword_fallback`
  - `fallback_reason in {llm_timeout,llm_invalid_json,llm_contract_invalid,llm_circuit_open}`
- 回退（传输）：
  - `planner_provider=keyword_fallback`
  - `fallback_reason=llm_http_error`
  - `plan_validation=ok`
- 指标：
  - 可观测字段完整：`total_requests/llm_preferred_requests/llm_success_requests/keyword_fallback_requests/retry_used_requests/retry_attempts/circuit_open_fallback_requests/consecutive_llm_failures`
- 安全：
  - 非白名单/风险不合法仍返回 `E4002/E4003`
  - 不发生真实写入

### 3.5 H3 预定 UE 命令（实现后执行）

- `HCIAbilityKit.AgentPlanWithRealLLMDemo "整理临时目录资产并归档"`
- `HCIAbilityKit.AgentPlanWithRealLLMDemo "扫描关卡里缺碰撞和默认材质"`
- `HCIAbilityKit.AgentPlanWithRealLLMDemo "把高面数网格做LOD检查"`
- `HCIAbilityKit.AgentPlanWithRealLLMProbe "你是谁"`（原始响应探针：`enable_thinking=false`）
- `HCIAbilityKit.AgentPlanWithRealLLMDemo config_missing "整理临时目录资产并归档"`（配置缺失回退）
- `HCIAbilityKit.AgentPlanWithRealLLMDemo http_fail "整理临时目录资产并归档"`（HTTP 异常回退）
- `HCIAbilityKit.AgentPlanWithLLMMetricsDump`

### 3.6 H3 当前状态（2026-02-24）

- 文档冻结：已完成。
- 代码实现：已完成（`Runtime HTTP Provider + Editor 命令 + 编译通过`）。
- UE 门禁：待用户执行 `3.5` 命令并反馈 `Pass/Fail`。
- 结论：本轮停在 `H3`，不跨到 I1。

## 4. 下一切片预案（仅占位，不提前实现）

### I1（紧随 H2）

- 目标：最小只读 UI（Plan 预览 + DryRunDiff + Locate）。
- 门禁：用户可完成“看计划 -> 看差异 -> 定位”闭环。

## 5. 变更纪律

- 范围变更先改 `00 + 05`。
- 契约变更先改 `03`。
- 结构变更才改 `04`。
- 每次门禁结果必须记录到 `文档/测试记录/`。
