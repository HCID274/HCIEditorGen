# StageH-SliceH5_E4303契约稳定性修复（待验证）

- 日期：2026-02-25
- 负责人：Codex + 用户 UE 手测
- 目标：降低 `E4303(llm_contract_invalid)` 触发，确保“整理临时目录”链路优先走真实 LLM 契约输出。

## 1. 测试步骤

1. 执行命令：`HCIAbilityKit.AgentPlanWithRealLLMDemo "整理临时目录资产并归档"`
2. 执行命令：`HCIAbilityKit.AgentPlanPreviewUI "整理临时目录资产并归档"`
3. 如出现 fallback，检查日志：`[HCIAbilityKit][AgentPlanLLM][E4303] ... detail=...`

## 2. 预期结果

1. `AgentPlanWithRealLLMDemo` 命中：`provider=llm`、`provider_mode=real_http`、`fallback_used=false`。
2. `AgentPlanPreviewUI` 命中：`SearchPath -> ScanAssets` discovery 链路，窗口正常打开。
3. 若失败，日志包含具体字段级错误（`code/field/reason`），不再只有笼统 `E4303`。

## 3. 实际结果

- 待用户 UE 手测填写。

## 4. 证据

- 待用户粘贴关键日志。

## 5. 结论

- 待判定（`Pass/Fail`）。
